# tex -> pdf, no ps
$pdf_mode = 1;

# cleanup (only delete .bll in cleanup if bib files all exist)
$bibtex_use = 1.5;
$clean_ext = 'bbl glo gls hd loa nav snm run.xml thm xdv';

$ENV{'TZ'}='America/Bahia';